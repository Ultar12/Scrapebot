# Render Blueprint for the Web Automation Web Service
services:
  - type: web
    name: pairing-code-service
    env: docker
    dockerfilePath: Dockerfile
    autoDeploy: false
    buildCommand: ""
    
    # CRITICAL: Gunicorn starts the Flask app, listening on the port provided by Render.
    startCommand: "gunicorn --bind 0.0.0.0:$PORT main:app" 

    envVars:
      # General Config (non-secret, value is explicit)
      - key: TARGET_URL
        value: https://levanter-delta.vercel.app/
        
      # 🔐 SECRETS: sync: false tells Render to manage the value securely 
      # in the dashboard. The 'value' field is omitted.
      - key: MOBILE_NUMBER
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
